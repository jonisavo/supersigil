---
title: Evidence Sources
description: How Supersigil turns criteria into covered, checkable evidence.
supersigil:
  id: website/concepts/evidence-sources
  type: documentation
---

import { Aside } from '@astrojs/starlight/components';

```supersigil-xml
<TrackedFiles paths="crates/supersigil-verify/src/explicit_evidence.rs, crates/supersigil-rust/src/**/*.rs" />
```

Coverage in Supersigil is not a vague statement about “having tests.” A
criterion is covered only when the graph can point to specific evidence for it.

## The Evidence Model

Every criterion starts as an uncovered verification target. It becomes covered
when Supersigil can resolve at least one evidence source for that criterion.

The built-in evidence paths are:

| Source | Best for | Strength |
|--------|----------|----------|
| Ecosystem plugins | Rust projects (and future language integrations) | Structured discovery from the code itself |
| `<VerifiedBy strategy="tag">` | Existing test suites in any language | Explicit mapping with very low setup cost |
| `<VerifiedBy strategy="file-glob">` | Broad file-level coverage | Fastest way to connect criteria to known test locations |
| `<Example verifies="...">` | API flows, CLI sessions, runnable docs | Documentation that doubles as evidence |

## Ecosystem Plugins

Plugins let Supersigil discover evidence directly from the code itself. The
Rust plugin is enabled by default and discovers `#[verifies]` annotations:

```rust
use supersigil_rust::verifies;

#[verifies("auth/req#valid-creds")]
#[test]
fn login_with_valid_credentials_returns_token() {
    // ...
}
```

The plugin parses test code and emits evidence records automatically. No
`<VerifiedBy>` component is needed in the spec — the annotation on the test
is sufficient.

## Tag-Based Evidence

For non-Rust projects, tags are a portable bridge from criteria to tests:

```xml
<Criterion id="valid-creds">
  WHEN a user submits valid credentials
  THE SYSTEM SHALL issue a session token.
  <VerifiedBy strategy="tag" tag="auth:valid-creds" />
</Criterion>
```

Then in a test file:

```rust
// supersigil: auth:valid-creds

#[test]
fn login_with_valid_credentials_returns_token() {
    // ...
}
```

Tag comments are recognized with `//`, `#`, `///`, `--`, and `/* */` comment
styles, so they work across languages.

## File-Glob Evidence

File globs trade precision for speed:

```xml
<VerifiedBy strategy="file-glob" paths="tests/auth/login_*.rs" />
```

This is useful when a criterion is verified by a small, stable set of files and
you do not need per-test granularity.

<Aside type="caution">
  File globs show that matching files exist; they do not tell you which specific
  test case inside those files satisfies the criterion.
</Aside>

## Executable Examples

Examples are ideal when the proof should live in the documentation itself:

````md
```http supersigil-ref=create-task
POST /api/v1/tasks
Content-Type: application/json

{"title": "Buy milk"}
```

```supersigil-xml
<Example id="create-task" runner="http" verifies="tasks/req/create#due-date">
  <Expected status="201" />
</Example>
```
````

Passing examples count as evidence during `supersigil verify`, so the same
system that checks structural integrity can also run and validate the workflow
you are documenting.

## Choosing the Right Source

- Use **plugins** for Rust projects — structured discovery with no manual linking.
- Use **tags** for non-Rust projects that want explicit criterion-to-test mapping.
- Use **file globs** when you only need coarse evidence and the test location is stable.
- Use **executable examples** when the docs themselves should be runnable proof.

## Related Reading

- [Verification](/supersigil/concepts/verification/) — Rule severities, draft gating, and exit codes.
- [Executable Examples](/supersigil/guides/executable-examples/) — Runner model and expected output matching.
- [CLI Commands](/supersigil/reference/cli/) — `context`, `status`, `refs`, and `verify`.
