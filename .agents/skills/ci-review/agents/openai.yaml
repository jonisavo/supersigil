interface:
  display_name: "CI & Review"
  short_description: "Supersigil verification in CI and PR review"
  default_prompt: "Use $ci-review to integrate Supersigil verification into CI pipelines, review PRs against spec coverage, or interpret verification output."
