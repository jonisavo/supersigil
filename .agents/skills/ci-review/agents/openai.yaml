interface:
  display_name: "CI & Review"
  short_description: "Supersigil verification in CI pipelines and PR reviews"
  default_prompt: "Use $ci-review to set up CI gates, review PRs against spec coverage, or interpret Supersigil verification output."
