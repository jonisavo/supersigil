---
supersigil:
  id: cli/tasks
  type: tasks
  status: draft
title: "CLI Tasks"
---

## Overview

Implementation tasks for the full CLI spec. Organized into phases by dependency order. Phase 1 covers cross-cutting infrastructure. Phases 2-4 add new commands. Phase 5 retrofits existing commands to the new output standards.

Existing implemented commands: `lint`, `ls`, `schema`, `context`, `plan`, `import`.
New commands to implement: `verify`, `status`, `affected`, `graph`, `init`, `new`.

## Phase 1: Cross-Cutting Output Infrastructure

<Task id="task-1-1" status="draft" implements="cli/req#req-14-2, cli/req#req-14-3">
  Add color/TTY detection module to `format.rs`. Implement `ColorConfig` struct
  that resolves the priority chain: `--color` flag > `FORCE_COLOR` > `NO_COLOR` >
  TTY auto-detection. Expose `use_color()` and `use_unicode()` methods.
</Task>

<Task id="task-1-2" status="draft" depends="task-1-1" implements="cli/req#req-14-4">
  Add Unicode/ASCII symbol helpers to `format.rs`. When color is disabled or stdout
  is not a TTY, use ASCII fallback symbols. Centralize symbol definitions so all
  commands use the same mapping.
</Task>

<Task id="task-1-3" status="draft" depends="task-1-1" implements="cli/req#req-14-1">
  Audit all existing commands for stdout/stderr discipline. Ensure data goes to
  stdout and diagnostics/errors go to stderr. Fix any violations.
</Task>

<Task id="task-1-4" status="draft" depends="task-1-1" implements="cli/req#req-14-5">
  Add next-step hint infrastructure. Create a helper that prints hints to stderr.
  Wire initial hints into existing commands (`lint` clean → suggest `verify`,
  error messages → suggest `ls` or `init`).
</Task>

<Task id="task-1-5" status="draft" depends="task-1-1" implements="cli/req#req-1-2">
  Update `ConfigNotFound` error message to include remediation guidance suggesting
  `supersigil init`.
</Task>

<Task id="task-1-6" status="draft" depends="task-1-2" implements="cli/req#req-15-1, cli/req#req-15-2, cli/req#req-15-3">
  Add `--color always|never|auto` global flag to clap. Thread `ColorConfig` through
  `main.rs` dispatch. Update exit code handling to support code 2 (reserved for
  `verify` warnings-only).
</Task>

## Phase 2: verify Command

<Task id="task-2-1" status="draft" depends="task-1-6" implements="cli/req#req-8-1, cli/req#req-8-2">
  Implement `commands/verify.rs`. Wire `load_graph()` → `supersigil_verify::verify()`
  → format output. Map CLI flags (`--project`, `--since`, `--committed-only`,
  `--merge-base`) to `VerifyOptions`.
</Task>

<Task id="task-2-2" status="draft" depends="task-2-1" implements="cli/req#req-8-3">
  Add `--format terminal|json|markdown` support to `verify`. Terminal format uses
  `supersigil_verify::format_terminal()` with the resolved `ColorConfig`. JSON and
  markdown use the verify crate's existing formatters.
</Task>

<Task id="task-2-3" status="draft" depends="task-2-2" implements="cli/req#req-8-4, cli/req#req-8-5, cli/req#req-8-6">
  Wire verify exit codes: `ResultStatus::Clean` → 0, `HasErrors` → 1,
  `WarningsOnly` → 2. Add remediation hint on errors ("Run `supersigil plan`...")
  and success message on clean ("N documents verified, no findings").
</Task>

## Phase 3: status, affected, graph Commands

<Task id="task-3-1" status="draft" depends="task-1-6" implements="cli/req#req-9-1, cli/req#req-9-2">
  Implement `commands/status.rs`. Project-wide mode: count documents by type/status,
  compute criteria coverage from graph. Per-document mode: call `graph.context(id)`
  for criteria/validation data, and extract `TrackedFiles`/`VerifiedBy` components
  directly from `graph.document(id).components` for tracked files and test mapping.
</Task>

<Task id="task-3-2" status="draft" depends="task-3-1" implements="cli/req#req-9-3, cli/req#req-9-4">
  Add `--format terminal|json` to `status`. Terminal output ends with next-step
  suggestions based on observed state.
</Task>

<Task id="task-3-3" status="draft" depends="task-1-6" implements="cli/req#req-10-1, cli/req#req-10-2, cli/req#req-10-3">
  Implement `commands/affected.rs`. Wire `load_graph()` →
  `supersigil_verify::affected::affected()`. Map CLI flags to function parameters.
</Task>

<Task id="task-3-4" status="draft" depends="task-3-3" implements="cli/req#req-10-4, cli/req#req-10-5">
  Add `--format terminal|json` to `affected`. Terminal format shows document ID,
  matched globs, and changed files. Handle empty results with a message.
</Task>

<Task id="task-3-5" status="draft" depends="task-1-6" implements="cli/req#req-11-1, cli/req#req-11-2, cli/req#req-11-3">
  Implement `commands/graph.rs`. Iterate `graph.documents()` for nodes. Extract
  ref components for edges. Output mermaid or graphviz dot format.
</Task>

## Phase 4: init and new Commands

<Task id="task-4-1" status="draft" depends="task-1-4" implements="cli/req#req-12-1, cli/req#req-12-2, cli/req#req-12-3">
  Implement `commands/init.rs`. Check for existing `supersigil.toml`. Write minimal
  config. Print next-step hint.
</Task>

<Task id="task-4-2" status="draft" depends="task-4-1" implements="cli/req#req-13-1, cli/req#req-13-2, cli/req#req-13-3">
  Implement `commands/new.rs`. Load config for type definitions. Derive file path
  from ID. Write MDX with front matter and type-appropriate placeholder components.
  Verify output passes lint. Print next-step hint.
</Task>

## Phase 5: Retrofit Existing Commands

<Task id="task-5-1" status="draft" depends="task-1-6" implements="cli/req#req-2-1, cli/req#req-2-2, cli/req#req-2-3, cli/req#req-2-4">
  Update `commands/lint.rs` to use `ColorConfig` for output formatting. Ensure
  lint errors show file path and position. Add next-step hint on clean ("Run
  `supersigil verify`...").
</Task>

<Task id="task-5-2" status="draft" depends="task-1-6" implements="cli/req#req-3-1, cli/req#req-3-2, cli/req#req-3-3, cli/req#req-3-4">
  Update `commands/ls.rs` to use `ColorConfig`. Verify stdout/stderr discipline
  and empty result behavior match criteria.
</Task>

<Task id="task-5-3" status="draft" depends="task-1-6" implements="cli/req#req-4-1, cli/req#req-4-2, cli/req#req-4-3">
  Update `commands/schema.rs` to use `ColorConfig` if applicable (schema output
  is primarily data, so this may be minimal).
</Task>

<Task id="task-5-4" status="draft" depends="task-1-6" implements="cli/req#req-5-1, cli/req#req-5-2, cli/req#req-5-3, cli/req#req-5-4">
  Update `commands/context.rs` to use `ColorConfig`. Ensure error on unknown ID
  suggests `supersigil ls`.
</Task>

<Task id="task-5-5" status="draft" depends="task-1-6" implements="cli/req#req-6-1, cli/req#req-6-2, cli/req#req-6-3, cli/req#req-6-4, cli/req#req-6-5">
  Update `commands/plan.rs` to use `ColorConfig`. Ensure prefix fallback and
  error messages suggest `supersigil ls`.
</Task>

<Task id="task-5-6" status="draft" depends="task-1-4" implements="cli/req#req-7-1, cli/req#req-7-2, cli/req#req-7-3, cli/req#req-7-4, cli/req#req-7-5">
  Update `commands/import.rs` to print next-step hint after successful import.
  Verify stdout/stderr discipline matches criteria.
</Task>
