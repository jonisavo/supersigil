/**
 * Entry point for the IIFE bundle used by IntelliJ's JCEF preview.
 * Exposes renderComponentTree and filterNovelEdges on
 * window.__supersigilRender.
 */
export { renderComponentTree, filterNovelEdges } from "./render.js";
